<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mylotterywheeler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyLotteryWheeler">
        <activity
            android:name=".ChoosePowerNumber"
            android:exported="false" />
        <activity
            android:name=".DisplayWheeledNumbers"
            android:exported="false" />
        <activity
            android:name=".SelectWheel"
            android:exported="false" />
        <activity
            android:name=".VerifyNumbers"
            android:exported="false" />
        <activity
            android:name=".EnterNumbers"
            android:exported="false" />
        <activity
            android:name=".ChooseNumDigits"
            android:label="@string/activity2_name"
            android:parentActivityName=".FullscreenActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.mylotterywheeler.FullscreenActivity" />
        </activity>
        <activity
            android:name=".FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyLotteryWheeler.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>